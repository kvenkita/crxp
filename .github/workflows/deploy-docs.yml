name: Deploy documentation to GitHub Pages

# Renders the application technical documentation (Quarto) and publishes it to GitHub Pages.
# Enable in repo Settings → Pages → Source: "GitHub Actions".
# (Note: GitHub Pages on a private repo requires a paid GitHub plan.)

on:
  push:
    branches: [master]
    paths:
      - 'docs/**'
      - '.github/workflows/deploy-docs.yml'
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: quarto-dev/quarto-actions/setup@v2
      - name: Render application technical documentation
        run: |
          quarto render docs/app-technical-documentation.qmd
          mkdir -p _site
          cp docs/app-technical-documentation.html _site/index.html
      - uses: actions/upload-pages-artifact@v3
        with:
          path: _site

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
